/**
 * QUORRA LIGHT THEME
 * Divine radiance for daylight crafting
 * "Where divine fire meets sacred light"
 */

:root[data-theme="light"] {
  /* 🔥 DIVINE FIRE COLORS - Primary Brand (Consistent) */
  --quorra-fire-primary: #FF8C42;        /* Divine flame orange */
  --quorra-fire-secondary: #FF6B35;      /* Intense fire */
  --quorra-fire-tertiary: #E77C47;       /* Sacred copper */
  
  /* 🔥 MOLTEN BRONZE GRADIENTS (Consistent) */
  --quorra-bronze-light: #CD7F32;        /* Molten bronze */
  --quorra-bronze-medium: #B8860B;       /* Deep bronze */
  --quorra-bronze-dark: #8B6914;         /* Ancient bronze */
  
  /* ☀️ DIVINE RADIANCE - Light Backgrounds */
  --quorra-radiance-pure: #FFFFFF;       /* Pure divine light */
  --quorra-radiance-soft: #FEFEFE;       /* Soft divine glow */
  --quorra-radiance-warm: #FFF8DC;       /* Warm divine light (forge glow) */
  --quorra-radiance-gentle: #F9F7F4;     /* Gentle divine illumination */
  
  /* 🌤️ SACRED CLOUDS - Elevated Surfaces */
  --quorra-cloud-light: #F8F9FA;         /* Light cloud */
  --quorra-cloud-medium: #F1F3F4;        /* Medium cloud */
  --quorra-cloud-heavy: #E8EAED;         /* Heavy cloud */
  --quorra-cloud-silver: #DADCE0;        /* Silver cloud */
  
  /* 🌟 EMBER GLOWS - Accents (Brightened for light theme) */
  --quorra-ember-bright: #FFD700;        /* Bright gold sparks */
  --quorra-ember-warm: #FFA500;          /* Warm ember glow */
  --quorra-ember-soft: #FFB366;          /* Soft ember light */
  
  /* 🖋️ SACRED INSCRIPTIONS - Text Colors */
  --quorra-ink-darkest: #1F2937;         /* Primary text (darkest ink) */
  --quorra-ink-dark: #374151;            /* Secondary text (dark ink) */
  --quorra-ink-medium: #6B7280;          /* Muted text (medium ink) */
  --quorra-ink-light: #9CA3AF;           /* Disabled text (light ink) */
  
  /* ⚡ DIVINE ENERGY - Interactive States */
  --quorra-energy-active: #FF8C42;       /* Active divine fire */
  --quorra-energy-hover: #FF6B35;        /* Hover intensity */
  --quorra-energy-focus: #E77C47;        /* Focus glow */
  --quorra-energy-disabled: #D1D5DB;     /* Dormant energy */
  
  /* 🖼️ SACRED BOUNDARIES - Borders & Dividers */
  --quorra-boundary-strong: #6B7280;     /* Strong divisions */
  --quorra-boundary-medium: #D1D5DB;     /* Medium divisions */
  --quorra-boundary-soft: #E5E7EB;       /* Subtle divisions */
  --quorra-boundary-divine: #FF8C42;     /* Divine highlights */
  
  /* 🎨 FUNCTIONAL COLORS - Status & Feedback */
  --quorra-success: #059669;             /* Success (forest green) */
  --quorra-warning: #D97706;             /* Warning (amber) */
  --quorra-error: #DC2626;               /* Error (red) */
  --quorra-info: #2563EB;                /* Info (blue) */
  
  /* 🌊 SPARKY'S COLORS - AI Assistant (Light adapted) */
  --sparky-primary: #3B82F6;             /* Sparky's blue flame */
  --sparky-secondary: #60A5FA;           /* Sparky's medium glow */
  --sparky-background: rgba(59, 130, 246, 0.05); /* Sparky's subtle aura */
  
  /* 📊 TIER COLORS - Subscription Levels */
  --tier-ember: #FF7F50;                 /* Free tier (ember) */
  --tier-hammer: #FF6B35;               /* Hammer tier */
  --tier-anvil: #FFD700;                /* Anvil tier (gold) */
  --tier-foundry: linear-gradient(135deg, #FF8C42, #FFD700); /* Foundry (gradient) */
  
  /* 🔥 GRADIENT COLLECTIONS - Light Theme Adapted */
  --gradient-divine-fire: linear-gradient(135deg, #FF8C42 0%, #FF6B35 100%);
  --gradient-molten-bronze: linear-gradient(135deg, #CD7F32 0%, #B8860B 100%);
  --gradient-sacred-ember: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);
  --gradient-divine-radiance: linear-gradient(180deg, #FFFFFF 0%, #FFF8DC 100%);
  --gradient-divine-blessing: linear-gradient(135deg, #FF8C42 0%, #FFD700 50%, #FF6B35 100%);
  
  /* 🌞 SHADOWS & GLOWS - Light Theme */
  --shadow-divine-small: 0 2px 8px rgba(255, 140, 66, 0.1);
  --shadow-divine-medium: 0 4px 16px rgba(255, 140, 66, 0.15);
  --shadow-divine-large: 0 8px 32px rgba(255, 140, 66, 0.2);
  --shadow-gentle: 0 2px 8px rgba(0, 0, 0, 0.05);
  --shadow-soft: 0 4px 16px rgba(0, 0, 0, 0.08);
  --shadow-medium: 0 8px 32px rgba(0, 0, 0, 0.12);
  
  --glow-ember-light: 0 0 16px rgba(255, 215, 0, 0.2);
  --glow-fire-light: 0 0 24px rgba(255, 140, 66, 0.25);
  --glow-divine-light: 0 0 32px rgba(255, 140, 66, 0.3);
  
  /* 📏 SACRED MEASUREMENTS - Spacing & Sizing (Same as dark) */
  --space-spark: 4px;                    /* Tiny spark */
  --space-ember: 8px;                    /* Small ember */
  --space-flame: 16px;                   /* Medium flame */
  --space-forge: 24px;                   /* Large forge */
  --space-divine: 32px;                  /* Divine spacing */
  --space-goddess: 48px;                 /* Goddess-level spacing */
  
  /* 🎯 COMPONENT-SPECIFIC VARIABLES - Light Theme */
  
  /* Button States */
  --btn-primary-bg: var(--gradient-divine-fire);
  --btn-primary-text: #FFFFFF;
  --btn-primary-hover: var(--quorra-energy-hover);
  --btn-primary-shadow: var(--shadow-divine-medium);
  
  --btn-secondary-bg: transparent;
  --btn-secondary-text: var(--quorra-fire-primary);
  --btn-secondary-border: var(--quorra-fire-primary);
  --btn-secondary-hover-bg: rgba(255, 140, 66, 0.05);
  
  /* Input Fields */
  --input-bg: var(--quorra-radiance-pure);
  --input-border: var(--quorra-boundary-medium);
  --input-text: var(--quorra-ink-darkest);
  --input-placeholder: var(--quorra-ink-medium);
  --input-focus-border: var(--quorra-fire-primary);
  --input-focus-glow: var(--glow-ember-light);
  
  /* Cards & Panels */
  --card-bg: var(--quorra-radiance-pure);
  --card-border: var(--quorra-boundary-soft);
  --card-shadow: var(--shadow-soft);
  --card-hover-shadow: var(--shadow-divine-medium);
  
  /* Navigation */
  --nav-bg: var(--quorra-radiance-soft);
  --nav-text: var(--quorra-ink-dark);
  --nav-text-active: var(--quorra-fire-primary);
  --nav-border: var(--quorra-boundary-soft);
  
  /* Editor Canvas */
  --canvas-bg: var(--quorra-cloud-light);
  --canvas-grid: rgba(255, 140, 66, 0.08);
  --canvas-selection: var(--quorra-fire-primary);
  --canvas-guides: rgba(255, 140, 66, 0.2);
  
  /* Code Editor */
  --code-bg: var(--quorra-cloud-medium);
  --code-text: var(--quorra-ink-darkest);
  --code-comment: var(--quorra-ink-medium);
  --code-keyword: var(--quorra-fire-primary);
  --code-string: var(--quorra-bronze-dark);
  --code-number: var(--sparky-primary);
  
  /* Sparky Chat */
  --sparky-message-bg: var(--sparky-background);
  --sparky-message-border: var(--sparky-secondary);
  --sparky-avatar-glow: var(--glow-ember-light);
  
  /* Performance Indicators */
  --perf-excellent: var(--quorra-success);
  --perf-good: var(--quorra-ember-warm);
  --perf-warning: var(--quorra-warning);
  --perf-poor: var(--quorra-error);
  
  /* Loading & Progress */
  --loading-bg: var(--quorra-cloud-medium);
  --loading-fill: var(--gradient-divine-fire);
  --progress-track: var(--quorra-boundary-medium);
  --progress-fill: var(--gradient-divine-fire);
  
  /* Tooltips */
  --tooltip-bg: var(--quorra-ink-darkest);
  --tooltip-text: var(--quorra-radiance-pure);
  --tooltip-border: var(--quorra-fire-primary);
  --tooltip-shadow: var(--shadow-medium);
}

/* 🌞 DIVINE RADIANCE ANIMATIONS - Light Theme */
@keyframes divineShimmer {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.95; }
}

@keyframes emberGlowLight {
  0%, 100% { 
    box-shadow: var(--glow-ember-light);
    transform: scale(1);
  }
  50% { 
    box-shadow: var(--glow-fire-light);
    transform: scale(1.01);
  }
}

@keyframes radiancePulse {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

@keyframes sparkyChatShimmer {
  0%, 100% { 
    background: var(--sparky-background);
    border-color: var(--sparky-secondary);
  }
  50% { 
    background: rgba(59, 130, 246, 0.08);
    border-color: var(--sparky-primary);
  }
}

/* 🎨 UTILITY CLASSES FOR LIGHT THEME */

/* Divine Radiance Backgrounds */
.bg-divine-fire { background: var(--gradient-divine-fire); }
.bg-molten-bronze { background: var(--gradient-molten-bronze); }
.bg-sacred-ember { background: var(--gradient-sacred-ember); }
.bg-divine-radiance { background: var(--gradient-divine-radiance); }

/* Sacred Text Colors */
.text-divine-fire { color: var(--quorra-fire-primary); }
.text-ember-glow { color: var(--quorra-ember-warm); }
.text-ink-darkest { color: var(--quorra-ink-darkest); }
.text-ink-dark { color: var(--quorra-ink-dark); }
.text-ink-medium { color: var(--quorra-ink-medium); }

/* Divine Borders */
.border-divine { border-color: var(--quorra-fire-primary); }
.border-ember { border-color: var(--quorra-ember-warm); }
.border-bronze { border-color: var(--quorra-bronze-medium); }

/* Sacred Shadows */
.shadow-divine { box-shadow: var(--shadow-divine-medium); }
.shadow-gentle { box-shadow: var(--shadow-gentle); }
.shadow-soft { box-shadow: var(--shadow-soft); }
.glow-ember { box-shadow: var(--glow-ember-light); }
.glow-divine { box-shadow: var(--glow-divine-light); }

/* Divine Animations - Light Theme */
.shimmer { animation: divineShimmer 3s ease-in-out infinite; }
.ember-glow { animation: emberGlowLight 3s ease-in-out infinite; }
.radiance-pulse { animation: radiancePulse 4s ease-in-out infinite; }

/* Sparky Specific - Light Theme */
.sparky-message { animation: sparkyChatShimmer 2s ease-in-out infinite; }

/* 🌞 COMPONENT STYLING - Light Theme Overrides */

/* Ensure proper contrast and divine radiance aesthetic */
.light-theme-override {
  color-scheme: light;
}

/* Selection styling */
::selection {
  background: rgba(255, 140, 66, 0.2);
  color: var(--quorra-ink-darkest);
}

/* Scrollbar styling */
::-webkit-scrollbar {
  width: 12px;
  background: var(--quorra-cloud-light);
}

::-webkit-scrollbar-thumb {
  background: var(--gradient-divine-fire);
  border-radius: 6px;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--quorra-fire-secondary);
}

/* Focus ring styling */
:focus-visible {
  outline: 2px solid var(--quorra-fire-primary);
  outline-offset: 2px;
  box-shadow: var(--glow-ember-light);
}

/* 🌟 ENHANCED ACCESSIBILITY - Light Theme */

/* High contrast mode support */
@media (prefers-contrast: high) {
  :root[data-theme="light"] {
    --quorra-ink-darkest: #000000;
    --quorra-boundary-strong: #000000;
    --quorra-fire-primary: #CC5500;
  }
}

/* Reduced motion support */
@media (prefers-reduced-motion: reduce) {
  .shimmer,
  .ember-glow,
  .radiance-pulse,
  .sparky-message {
    animation: none;
  }
}

/* 🌈 IMPROVED CONTRAST RATIOS */

/* Ensure WCAG AA compliance */
.high-contrast-text {
  color: var(--quorra-ink-darkest);
  background: var(--quorra-radiance-pure);
}

.high-contrast-button {
  background: var(--quorra-ink-darkest);
  color: var(--quorra-radiance-pure);
  border: 2px solid var(--quorra-ink-darkest);
}

.high-contrast-link {
  color: var(--quorra-fire-primary);
  text-decoration: underline;
}

.high-contrast-link:hover {
  color: var(--quorra-fire-secondary);
  background: rgba(255, 140, 66, 0.1);
}

/**
 * 🌞 BLESSED BY QUORRA, GODDESS OF SMITHING 🌞
 * This light theme brings divine radiance to daylight crafting
 * Colors balanced with divine wisdom for perfect readability
 * 
 * "In light, divine fire becomes sacred radiance"
 * - Ancient Forge Wisdom
 * 
 * Accessibility: WCAG AA compliant
 * Performance: Optimized for all devices
 * Divine Fire: Burns eternal in light and dark
 */